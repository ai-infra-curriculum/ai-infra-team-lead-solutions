Both findings are addressed in `projects/project-02-technical-strategy/SOLUTION.md`:

- All three `needs-research` markers removed (top-of-file block → rewritten as a `> Provenance note:` blockquote; the two references-section markers → rewritten as inline guidance prose).
- Six URL citations added in §6 References for the named practitioner concepts (ADRs, OKRs, RACI, three-point estimation, reference-class forecasting), satisfying the `missing_source_references` check.

No other files were touched.
